<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- No INTERNET permission: the app is fully offline, all assets are
         bundled in the APK. -->

    <!-- Haptic feedback on cell taps. Normal permission: no runtime prompt,
         no data-safety impact. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Media3 ExoPlayer merges this in for network-requirement watching; the
         app plays bundled assets only, so strip it to keep the hard offline
         guarantee (VIBRATE must stay the only permission). -->
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:node="remove" />

    <!-- Mark hardware as optional so emulators (BlueStacks, NoxPlayer, AVDs)
         don't get filtered out on Play. BlueStacks reports `faketouch`
         instead of `touchscreen` and lacks real telephony/GPS hardware. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.screen.portrait"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.screen.landscape"
        android:required="false" />

    <application
        android:name=".LotoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Loto">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Loto.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
